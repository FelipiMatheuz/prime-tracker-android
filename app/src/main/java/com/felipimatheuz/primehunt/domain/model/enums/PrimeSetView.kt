package com.felipimatheuz.primehunt.domain.model.enums

import com.felipimatheuz.primehunt.R

enum class PrimeSetView(val displayNameRes: Int) {
    COLLECTIONS(R.string.tab_collections),
    PRIME_SETS(R.string.tab_prime_sets)
}